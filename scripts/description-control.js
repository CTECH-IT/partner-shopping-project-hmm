const BUTTON_ID = '[class="shopping-item"]';
/*
function initializeLists() { //gets every button element, and then sets the description toggle to false
    let elArray = [].call.slice(document.querySelectorAll(BUTTON_ID));
    let storeArray;
    for (let i of elArray) {
        storeArray.push({i:false});
    }
    return storeArray;
}
let buttonModes = initializeLists();
*/
function toggleDesc() {
    
}